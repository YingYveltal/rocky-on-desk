<svg xmlns="http://www.w3.org/2000/svg" viewBox="-15 -25 45 45" width="500" height="500">
  <defs>
    <style>
      .body-walk {
        transform-origin: 7.5px 15px;
        animation: walk-bob 0.8s infinite ease-in-out;
      }
      .leg1 { transform-origin: 3px 11px; animation: leg-step 0.8s infinite ease-in-out; }
      .leg2 { transform-origin: 5px 12px; animation: leg-step 0.8s infinite ease-in-out; animation-delay: -0.4s; }
      .leg3 { transform-origin: 9px 12px; animation: leg-step 0.8s infinite ease-in-out; animation-delay: -0.4s; }
      .leg4 { transform-origin: 12px 11px; animation: leg-step 0.8s infinite ease-in-out; }
      .arm-pump { transform-origin: 4px 5px; animation: arm-pump 0.8s infinite ease-in-out; }
      .crystal-active { animation: active-glow 1.6s infinite ease-in-out; }
      .dust { opacity: 0; }
      .d1 { animation: dust-puff 0.8s infinite; }
      .d2 { animation: dust-puff 0.8s infinite; animation-delay: 0.4s; }

      @keyframes walk-bob {
        0%, 50%, 100% { transform: translateY(0); }
        25%, 75%      { transform: translateY(-0.6px); }
      }
      @keyframes leg-step {
        0%, 100% { transform: translateY(0); }
        50%      { transform: translateY(-1.2px); }
      }
      @keyframes arm-pump {
        0%, 100% { transform: rotate(0deg); }
        50%      { transform: rotate(-10deg); }
      }
      @keyframes active-glow {
        0%, 100% { opacity: 1; }
        50%      { opacity: 0.7; }
      }
      @keyframes dust-puff {
        0%   { opacity: 0; transform: translate(0, 0) scale(0.5); }
        30%  { opacity: 0.6; }
        100% { opacity: 0; transform: translate(-2px, -1px) scale(1.2); }
      }
    </style>
  </defs>

  <rect x="-1" y="15" width="17" height="1" fill="#000000" opacity="0.5"/>

  <!-- Dust puffs behind feet -->
  <g fill="#C49A6C">
    <rect class="dust d1" x="0"  y="14" width="1" height="1"/>
    <rect class="dust d2" x="13" y="14" width="1" height="1"/>
  </g>

  <g class="body-walk">
    <g fill="#A47148">
      <rect class="leg1" x="2"  y="11" width="2" height="4"/>
      <rect class="leg2" x="5"  y="12" width="1" height="3"/>
      <rect class="leg3" x="9"  y="12" width="1" height="3"/>
      <rect class="leg4" x="11" y="11" width="2" height="4"/>
    </g>

    <g class="arm-pump">
      <g fill="#A47148">
        <rect x="2" y="3"  width="2" height="3"/>
        <rect x="3" y="1"  width="2" height="2"/>
        <rect x="3" y="-1" width="2" height="2"/>
      </g>
    </g>

    <rect x="13" y="7" width="2" height="2" fill="#A47148"/>
    <rect x="2" y="5" width="11" height="7" fill="#A47148"/>

    <g fill="#6B4A2D">
      <rect x="2" y="8"  width="11" height="0.4"/>
      <rect x="6" y="5"  width="0.4" height="3"/>
      <rect x="9" y="8.4" width="0.4" height="3.6"/>
    </g>

    <g fill="#C49A6C">
      <rect x="3"  y="5" width="2" height="1"/>
      <rect x="8"  y="5" width="2" height="1"/>
    </g>

    <g class="crystal-active" fill="#5DBF7E">
      <rect x="4"  y="6"  width="1" height="1"/>
      <rect x="10" y="6"  width="1" height="1"/>
      <rect x="6"  y="10" width="2" height="1"/>
    </g>

    <!-- Determined eyes (slightly narrowed) -->
    <g fill="#1A1A1A">
      <rect x="4"  y="8.5" width="1" height="1.5"/>
      <rect x="10" y="8.5" width="1" height="1.5"/>
    </g>
  </g>
</svg>
